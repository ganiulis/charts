apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-test
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-test
    image: {{ .Values.container.name }}
    command: ["sh", "-c", "pg_isready -h {{ .Values.container.name }} -p 5432 -t 5"]
  restartPolicy: Never
